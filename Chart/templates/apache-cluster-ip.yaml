apiVersion: v1
kind: Service
metadata:
  labels:
    app: apache-service
  name: apache-service
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: {{ .Release.Name }}-apache
